import React from "react";
import { AppBar, Container } from "@material-ui/core";

export default function Header() {
  return (
    <AppBar>
      <Container></Container>
    </AppBar>
  );
}
